# good-lock

## Build Docker

```bash
# build imgs (same directory as docker-compose.yml)
$ docker compose build

# Start containers
$ docker compose up -d
```
## Back 
```bash
# start server
$ docker exec back yard dev
```